package com.blks.receiver;

import android.content.BroadcastReceiver;
import android.content.Context;
import android.content.Intent;

public class AlarmReceiver extends BroadcastReceiver {
    @Override
    public void onReceive(Context context, Intent intent) {

//        Intent heartIntent = new Intent(context, HeartService.class);
//        context.startService(heartIntent);
//
//        Intent siteIntent = new Intent(context, SiteService.class);
//        context.startService(siteIntent);
//
//        Intent taskIntent = new Intent(context, TaskService.class);
//        context.startService(taskIntent);
    }
}
